# Custom Scalars
scalar Date

type Query {
  users: [User]!
  user(id: ID!): User
}

type Mutation {
  # Authors
  createUser(user: UserCreateInput!): User!
  updateUser(id: ID!, user: UserUpdateInput!): User!
}
