type Query {
  users: [PublicUser]!
  user(id: ID!): PublicUser
  login(email: String!, password: String!): LoggedInUser
}

type Mutation {
  createUser(user: UserCreateInput!): PublicUser!
  updateUser(id: ID!, user: UserUpdateInput!): PublicUser!
}
